% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{RunEvent}
\alias{RunEvent}
\title{OpenLineage run event}
\usage{
RunEvent(
  run,
  job,
  event_type = NULL,
  event_time = new_event_time(),
  inputs = list(),
  outputs = list(),
  producer = OPENLINEAGE_PRODUCER,
  schema_url = OPENLINEAGE_RUN_EVENT_SCHEMA_URL
)
}
\arguments{
\item{run}{A \code{Run} object.}

\item{job}{A \code{Job} object.}

\item{event_type}{A \code{RunState}, a valid state string, or \code{NULL} to omit it.}

\item{event_time}{An RFC 3339 date-time string with a time-zone offset.}

\item{inputs}{An unnamed list of \code{InputDataset} objects, or \code{NULL}.}

\item{outputs}{An unnamed list of \code{OutputDataset} objects, or \code{NULL}.}

\item{producer}{A URI identifying the event producer.}

\item{schema_url}{The RunEvent JSON Schema URL.}
}
\value{
A \code{RunEvent} S7 object.
}
\description{
Describes a lifecycle transition for one job run and its input and output
datasets.
}
\examples{
run <- Run(new_run_id())
job <- Job("example-scheduler", "daily-report")
RunEvent(
  run,
  job,
  event_type = "START",
  event_time = new_event_time(as.POSIXct("2026-01-02", tz = "UTC"))
)
}
